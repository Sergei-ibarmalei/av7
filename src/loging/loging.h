#ifndef LOGING_H
#define LOGING_H
#include <string>
#include <iostream>

/*namespace log_
{
    using string_ = std::string;

    struct LogNode
    {
        string_* info;
        struct LogNode* next = nullptr;
    };

    using Node = struct LogNode;

    class Log
    {
        private:
        Node* first;
        Node* current;
        void showInfo(Node* first);
        void deleting(Node* first);
        public:

        string_ log_info; 
        explicit Log();
        ~Log();
        Log(const Log& ) = delete;
        Log& operator=(const Log& ) = delete;
        void push(const string_& log_info);
        void Show();

    };
}*/



#endif